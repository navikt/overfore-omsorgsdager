import React from 'react';
import { FormattedMessage } from 'react-intl';

const OverføringInfo = () => (
    <>
        <p>
            <FormattedMessage id="info.overføring.1" />
        </p>

        <p>
            <FormattedMessage id="info.overføring.2" />
        </p>

        <p>
            <FormattedMessage id="info.overføring.3" />
        </p>
    </>
);

export default OverføringInfo;
